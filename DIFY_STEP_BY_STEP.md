# 青少版 Chatflow · 5 节点操作手册

> 注：成年版 5 节点已于 8/3 完整跑通（见对话），本手册**专指青少版**。
> 你刚跑通的是「乐天知性·青少版」基础骨架（用户输入 → LLM Step 3.7 Flash）。
> 目标：补成完整 5 节点：`开始 → 知识检索(kb_minor) → 条件分支(成人话题拦截) → LLM → 直接回复`。

---

## ⚠️ 常见错误 · 必看（8/4 踩坑实录）

**症状**：发「安全期是哪几天」这种正常问题，命中"直接回复 2"（成人版拦截话术）

**根因**：条件分支的「下一步」路由配反了——
- 配错：IF（含成人词）→ LLM；ELSE（干净）→ 直接回复
- 应该：IF（含成人词）→ 直接回复（拦截）；ELSE（干净）→ LLM

**记忆口诀**：「**IF 命中拦截，ELSE 干净放行**」—— 防护节点永远要拦 IF、放过 ELSE。

**两种修法（任选）**：
1. 把 `包含` 改成 `不包含`，路由保持 IF→LLM, ELSE→直接回复
2. 条件保持 `包含`，交换 IF/ELSE 的下一步：IF→直接回复, ELSE→LLM

**验证**：改完测两条——「安全期是哪几天」应回 LLM 答案；「做爱」应回拦截话。

### 坑 2 · 多个关键词 ≠ OR 匹配

**症状**：「怎么做爱」这种含关键词的 query 仍漏到 LLM，StepFun 拒答返回 400

**根因**：Dify 条件分支的「包含/不包含」是**整段子串匹配**，不是 token 列表 OR 匹配。

你写：`IF query 不包含 "做爱 性技巧 口交 约炮 色情 成人电影 成人内容"`
- 实际语义：不包含「做爱 性技巧 口交 约炮 色情 成人电影 成人内容」这一个长字符串
- 「怎么做爱」不含这个长字符串 → 条件 TRUE → 漏到 LLM

**修法**：用「`+ ELIF`」一个关键词一个分支串行
```
IF    query 包含 "做爱"      ─→ 直接回复 2
ELIF  query 包含 "性技巧"    ─→ 直接回复 2
ELIF  query 包含 "口交"      ─→ 直接回复 2
ELIF  query 包含 "约炮"      ─→ 直接回复 2
ELIF  query 包含 "色情"      ─→ 直接回复 2
ELIF  query 包含 "成人电影"   ─→ 直接回复 2
ELIF  query 包含 "成人内容"   ─→ 直接回复 2
ELSE                         ─→ LLM
```

> ⚠️ 不能用「IF + 添加条件」堆多条——那是 AND 语义（全部满足才 IF），不是 OR。

---

## 一、青少版 vs 成年版 关键差异（务必看）

| 维度 | 成年版 | 青少版（本次） |
|------|--------|----------------|
| 知识库 | `kb_adult` | **`kb_minor`**（先 12-15 段，ITGSE 简化 + 中国学校课本） |
| SYSTEM 提示词 | `SYSTEM_PROMPT.md`（2557 字符） | **`SYSTEM_PROMPT_MINOR.md`（3676 字符，含 NO_MINOR_SEXUALIZATION）** |
| 条件分支触发 | 含"未成年/自残"等 → ELSE 年龄门拒绝 | **含"成人色情/性技巧/具体性行为" → 温和引导到健康层** |
| 防护核心 | 18+ 年龄闸 | **未成年人性化内容零容忍（不答成人细节，只聊身体权/健康/边界）** |
| ELSE 文案 | "请跳转青少版" | "这个问题属于成人版，我们先聊聊身体权和健康边界～" |

---

## 二、最终链路长这样（截图保存）

```
┌──────┐   ┌──────────────┐   ┌──────────────────┐   ┌────────┐   ┌──────────────┐
│ 开始 │ → │ 知识检索      │ → │ 条件分支          │ → │ LLM     │ → │ 直接回复      │
│ 节点 │   │ kb_minor     │   │ 成人话题命中？    │   │ Step3.7 │   │ (送 LLM 输出) │
│      │   │ (top_k=5)    │   │ IF / ELSE        │   │ Flash   │   │              │
└──────┘   └──────────────┘   └──────────────────┘   └────────┘   └──────────────┘
                                          │
                                          ↓ ELSE 分支（成人话题命中）
                                 ┌─────────────────────────┐
                                 │ 直接回复（温和引导）     │
                                 │ 「这个问题属于成人版～  │
                                 │  我们先聊聊身体权和     │
                                 │  健康边界吧」           │
                                 └─────────────────────────┘
```

---

## 三、一步步操作（你按 1→7 点）

### 步骤 1 · 新建知识库 `kb_minor`

1. 左侧「知识库」→「创建知识库」→ 命名 **`kb_minor`**（必须叫这个）
2. 索引模式选「高质量」/ `text-embedding-3-small`
3. 检索：top_k=5，score_threshold=0.5
4. **上传文档按 `KB_MINOR_UPLOAD_LIST.md` 清单**（已给，含真实 URL + URL直抓/本地传 两种方式）

### 步骤 2 · 加「知识检索」节点（接 kb_minor）

1. 在 LLM 节点左边线拖出空节点 → 选「知识检索」
2. 双击：
   - 知识库选 **`kb_minor`**
   - 查询变量选 `sys.query`
   - 输出变量默认 `result`
3. 删掉「用户输入 → LLM」原连线，改成「用户输入 → 知识检索」
4. 保存

### 步骤 3 · 加「条件分支」节点（成人话题拦截）

⚠️ 关键：每个关键词独立 ELIF 分支（详见顶部「坑 2」），不能堆一条 IF 里。

1. 知识检索右边拖新节点 → 选「条件分支」
2. 双击，按这个结构逐条加（`+ ELIF` 按钮点 6 次）：
   ```
   IF    (x) query 包含 "做爱"      ─→ 直接回复 2
   ELIF  query 包含 "性技巧"        ─→ 直接回复 2
   ELIF  query 包含 "口交"          ─→ 直接回复 2
   ELIF  query 包含 "约炮"          ─→ 直接回复 2
   ELIF  query 包含 "色情"          ─→ 直接回复 2
   ELIF  query 包含 "成人电影"       ─→ 直接回复 2
   ELIF  query 包含 "成人内容"       ─→ 直接回复 2
   ELSE                           ─→ LLM
   ```
3. 每条 IF/ELIF 路由都连到下方「直接回复 2」（拦截节点，步骤 5）
4. ELSE 路由连到上方 LLM（步骤 4）
5. 把「知识检索」连进条件分支入口
6. 保存

### 步骤 4 · LLM 节点改设置

1. 双击「LLM」节点
2. 模型保持 `Step 3.7 Flash CHAT`
3. **SYSTEM 框删空，从本地 `SYSTEM_PROMPT_MINOR.md`（3676 字符）全量粘贴**（Dify 可装 8000 字符，够）
4. USER 框填 `{{#sys.query#}}`
5. 上下文勾选启用，变量填 `{{#knowledge_retrieval.result#}}`
6. 把 LLM 连到条件分支 **ELSE 路线**（干净 query → LLM 处理；命中成人词走 IF → 直接回复拦截）
7. 保存

### 步骤 5 · IF 分支「直接回复」节点（温和引导）

1. 条件分支下方拖「直接回复」
2. 双击内容填：
   > 这个问题属于成人版内容～ 我们先聊聊身体权和健康边界吧。如果你好奇的是「怎么保护自己」「怎么和父母沟通」「青春期身体变化」，我都可以帮你 ❤️
3. 把条件分支 **IF 分支**连到这个节点（命中成人词 → 拦截）
4. 保存

### 步骤 6 · ELSE 路线「直接回复」节点（送 LLM 输出）

1. LLM 右边拖「直接回复」
2. 内容填 `{{#llm.text#}}`
3. 把 LLM 连到这个节点
4. 保存

### 步骤 7 · 连线汇总 + 预览测试

```
开始 → 知识检索 → 条件分支
              ├─IF（含成人词）─→ 直接回复（温和引导文案）[拦截]
              └─ELSE（干净）───→ LLM ─→ 直接回复（LLM 输出）[放行]
```

预览测 3 条：

| 输入 | 期望 |
|------|------|
| "安全期是哪几天？" | ELSE 路线 → LLM 引用 ITGSE/纲要 回答（即使 kb_minor 空也返回"未检索到"说明链路通） |
| "做爱的时候怎么保护自己？" | IF 分支（成人话题命中）→ 返回温和引导文案（不答怎么做爱） |
| "怎么和爸妈聊青春期变化？" | ELSE 路线 → 返回 LLM 答案 |

---

## 四、上传 kb_minor 文档（详细清单见 `KB_MINOR_UPLOAD_LIST.md`）

快速版 5 篇核心（P0/P1，今天就能下）：

| 序 | 文件名 | 来源 |
|---|------|------|
| 1 | `ITGSE_国际性教育技术指导纲要_中文` | UNFPA 中国（URL 直抓） |
| 2 | `中小学健康教育指导纲要_2008` | 国务院公报（URL 直抓） |
| 3 | `生命安全与健康教育进中小学课程教材指南_2021` | 教育部（URL 直抓） |
| 4 | `刘文利_全面性教育课程要点` | 中国大学 MOOC |
| 5 | `玛丽斯特普_你我伙伴_青少课程包` | marystopes.cn |

⚠️ 红线：绝不灌成人性技巧/色情细节；公众号文章用「打印→另存 PDF」再传。

---

## 五、拿到青少版 Dify App API Key

青少版跑通后：

1. 左侧「访问 API」→ 复制 Key（形如 `app-xxxxxxxx`）
2. 发我一次（我本地 `export` 用，不写文件）
3. 我跑：

```bash
python eval_runner.py --suite eval_testset.csv --version minor --out scores/minor_baseline.json
python eval_runner.py --suite redteam_prompts.csv --version minor --out scores/minor_redteam.json
```

---

## 六、今天成功标准

- ✅ 5 节点视觉完整（知识检索/条件分支/2 个直接回复）
- ✅ 步骤 7 三条测试符合预期
- ✅ kb_minor 有 5 篇核心文档
- ✅ 青少版 App Key 拿到发我
- ✅ E3 课程体系 + E4 demo 页方向拍板（见同目录另两份文件）
