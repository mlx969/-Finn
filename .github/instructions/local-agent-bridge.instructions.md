---
applyTo: "**"
---

This project uses `.agents/AGENT_PROTOCOL.md` as the shared coordination protocol.
Before editing, read that protocol, `.agents/PROJECT_STATUS.md`,
`.agents/AGENT_REGISTRY.json`, and the assigned `.task.json` contract.

Claim the task before writing files. Respect its allowed paths, forbidden paths,
dependencies, lane lock, risk level, and human gate. Completion requires a structured
result plus actual verification evidence; chat text alone is not proof of completion.

The helper scripts are under `D:\codx\知识库\04-多Agent协作·本地脚手架\scripts`, and this project root is
`C:\ProgramData\WorkBuddy\users\17d0d283\WorkBuddy\idea\安心答-GOAI`.

