# Dify 搭建指南（截图级）+ 操作边界说明

> 配套：本目录 `SYSTEM_PROMPT.md`（成年版）、`SYSTEM_PROMPT_MINOR.md`（未成年版，待建）、`KNOWLEDGE_BASE.md`、`EVAL_REDTEAM.md`
> 目标：在 Dify 上搭出可提交的「乐天知性」AI 应用，并在 GOAI 初赛（8/16）前接入真实模型。
> 📌 **本版 = Day 8「照着点」终版（2026-08-10 晚 心跳补强）**：已把 A5（kb_adult 检索全空）的修复内联进 Step 4，并新增 §4.5 验收清单 + 跑分交接。配套故障排查见 `KB_ADULT_REPAIR_GUIDE.md`。

---

## 0. 我能做 vs 不能做（先说清边界）

| 事项 | 我（AI）能做吗 | 说明 |
|---|---|---|
| 写精确到每一步的截图级搭建指南 | ✅ 能 | 本文档即是 |
| 准备好所有要粘贴的内容（提示词/知识库清单/模型参数） | ✅ 能 | 已在本目录备齐 |
| 替你**登录 Dify 账号**、在云端 UI 点按钮创建应用 | ❌ **不能** | Dify 是你的外部账号，涉及凭证；且当前环境没有 Dify 连接器，我无权操作你的云端资源 |
| 用脚本帮你**自动跑评测**（读测试集→调 API→出分数） | ✅ 能（前提：你给 **Dify 应用 API Key**） | 这个 Key 是「应用调用密钥」，给到我后我只会存在本次会话内存跑脚本，绝不落库、绝不外传 |
| 直接部署到你的 Cloudflare/服务器 | ❌ **不能** | 同上，属你的外部资源，需你授权后你自己操作 |

**结论**：**搭建动作得你来**（你的账号、你的 Key），但我会把「你只需照着点」的指南和全部粘贴物备好，10 分钟能搭完。评测实跑可以交给我（你给应用 API Key 即可）。

---

## 0.5 difyctl（v1.15.0 官方 CLI）角色澄清

> 用户问：「给你接 difyctl 会有帮助部署吗？」——结论：**对「搭建/部署」无帮助，对「评测自动化」有价值，但本项目的更优路径是 Dify App API Key。**

**difyctl 官方定位（docs.dify.ai/zh/cli/overview 原话）**：
> "你在浏览器里构建 Dify 应用，但真正用到它们的场景往往在别处……difyctl 正是为此而生：只要能运行 Shell 命令，就能运行你的 Dify 应用。"

**能力边界（已核实）**：
| 能力 | 支持 | 说明 |
|---|---|---|
| 运行已建好的应用、取输出（流式/JSON） | ✅ | 适合脚本化批量调用 |
| 列出工作区应用、查看输入 | ✅ | 发现应用 |
| 让 AI Agent 把 Dify 应用当工具调用 | ✅ | 需 difyctl + "integrate-agents" 配置 |
| **创建 / 导入 / 部署应用拓扑** | ❌ | 文档明确"在浏览器里构建"，CLI 不建应用 |
| 非交互式 API Key 调用 | ❌ | 它用 **OAuth 登录一次**缓存 token，需交互式浏览器登录 |

**对我们项目的影响**：
1. **部署/搭建**：difyctl **不能**替你在 Dify 画布上导入 SYSTEM_PROMPT、配知识库、选模型——那一步永远得你在 Web UI 做（见 §2/§3）。difyctl 不改变这条。
2. **评测自动化**：应用发布后，difyctl 能批量调用它跑评测。但它需要 **OAuth 交互登录**，我（沙箱、无浏览器、无 Dify 连接器）没法在环境里完成登录 → 实际接不通。
3. **更优路径 = Dify App API Key**（§4）：你在应用「API 访问」生成一串 secret，发我（仅内存不落库），我写 `eval_runner.py` 直接 `POST /v1/chat-messages` 批量跑 152+47 条 → 出三组对照分。**无需 CLI、无需浏览器登录、比 difyctl 更轻**。
4. **difyctl 的 `--think`**（思维链展示）对报告截图是锦上添花，但非必需；`/create` 一句话生成工作流是 **Web UI 全局搜索框功能**，不是 CLI，我无法直接触发。

**决策**：本项目评测实跑走「App API Key + Python 脚本」路径；difyctl 仅作为你本机可选的可视化/演示工具，不纳入必要流程。

---

## 1. 前置准备

- [ ] **Dify 账号**：cloud.dify.ai 注册（或本地 docker 部署，适合「隐私不落库」叙事）
- [ ] **阶跃 API Key**：纯 ASCII（已从 `index.html` 加了编码校验，复制时别带中文/全角）
- [ ] **加入 Datawhale 夏令营群**：拿赛道 baseline 与答疑（确认入群后告诉我）
- [ ] 本目录文件已就绪：`SYSTEM_PROMPT.md`、`KNOWLEDGE_BASE.md`

---

## 2. 成年版搭建（截图级步骤）

### Step 1 · 创建应用（**选 Chatflow**）
1. 登录 Dify → 右上「**创建应用**」→ **选「Chatflow」（"支持记忆的复杂多轮对话工作流"）**——不要选 Agent
2. 名称填：`乐天知性·成年版`；图标可选 💜
3. 描述：`18+ 性健康与亲密关系 AI 助手｜升维视角｜GOAI 参赛作品`

> **为什么选 Chatflow 不是 Agent**（用户截图确认决策）
> - 涉性+未成年场景需**确定性**安全拦截，Agent 自主推理会绕开红线
> - 升维视角的多轮流程需**记忆 + 显式节点控制**（先共情→问背景→再升维）
> - 风险转介（自伤/家暴）必须走**预设节点**，不能交给 Agent 自主决定
> - 评测三组对照需**稳定输出**，Agent 输出方差大
>
> **Chatflow 节点结构**（照搬搭建）：
> ```
> [开始] → 身份分流（本期先成年版）→ [关键词过滤]（命中红线→拒答）
>       → [LLM1:意图分类] → [ShieldLM安全二审]
>       → [kb_adult 检索] → [LLM2:主对话，注入 SYSTEM_PROMPT]
>       → [风险转介节点]（自伤/家暴→预设话术，不走LLM）→ [结束]
> ```

### Step 2 · 粘贴 System Prompt（Chatflow 关键）
> ⚠️ Chatflow **没有独立「提示词」标签页**（那是"聊天助手"才有）。系统提示词是填在 **LLM 节点**上的。

1. 进入应用编排画布，找到 **LLM 节点**（默认名「LLM」，带 AI 图标）。
   - 若画布只有「开始 + 直接回答」没有 LLM：从左侧节点栏拖一个 **LLM** 节点，连线成「开始 → LLM → 直接回答」。
2. **点 LLM 节点** → 右侧配置面板顶部有 **SYSTEM（系统提示词）** 输入框 → 把本目录 `SYSTEM_PROMPT.md` **全文**粘进去（含角色 / 升维视角 / 7 条安全规则）。
3. 节点下方「PROMPT（用户消息）」区默认是 `{{#sys.query#}}`（用户消息变量），**保留不动**。
4. 模型参数：Temperature `0.7`、Max Tokens `1500`、开启「对话开场白」。

### Step 3 · 绑定阶跃模型
1. 「**模型供应商**」→ 添加「**OpenAI 兼容**」类（或自定义 StepFun）
2. API Key：填你的阶跃 Key（**纯 ASCII**，不要含中文/全角符号/换行；粘贴前用纯键盘 Ctrl+C 复制）
3. API 地址（Base URL）：`https://api.stepfun.com/step_plan/v1`
4. 模型名：**`Step 3.7 Flash`** ← 你截图里就是这个（比 step-router-v1 更省 token、更快，MVP 阶段最合适）
5. 保存并「**发布**」（左侧"发布"按钮）

> ⚠️ **如果模型下拉里没看到 Step 3.7 Flash**：说明模型供应商配置里没填 Base URL，或者只填了 Key 没填模型名。回「模型供应商 → StepFun 编辑」核对 API 路径和模型名。

### Step 4 · 建知识库（RAG）⚠️ 这一步做错 = A5 检索全空，务必照做

> **血泪教训（A5 根因）**：`kb_adult` 一旦切成「**高质量**」索引模式，Dify 会要求一个 embedding 模型，默认指向 Google Gemini 的 `models/embedding-001`，你环境里没配 → 整条工作流 404 崩，所有回答走兜底（`retriever_resources: []`）。青少版没事是因为它用「经济模式」。**所以：新 KB 一律选「经济模式」，不要碰「高质量」。**

1. 「**知识库**」→ 新建 `kb_adult`
   - 名称必须叫 `kb_adult`（Chatflow 节点绑定按名字，改名会断）
   - **索引模式：勾「经济」**（默认就是经济，确认别误勾高质量）
   - 嵌入模型自动锁定 "embedding-3-small / Dify 自带"——**不要改**
   - 检索设置：`top_k` 设 **3**、`score_threshold` 设 **0.5**（覆盖默认 5 / 0.5）
2. 「**新增文档**」→ 把本地两个已备好的 md 拖进去（路径：`安心答-GOAI/kb_adult_uploads/`）：
   - `01_zhongguo_gongmin_jiangkang_suyang_2024.md`（中国公民健康素养 2024）
   - `03_liaoning_nhc_kexue_biyun_jiangzuo.md`（辽宁卫健科学生育讲座）
   - 处理模式「自动」，等状态从「索引中」变「**已完成**」（约 3–5 分钟，别在"处理中"就测）
3. **回应用编排 → 双击「知识检索」节点** → 知识库下拉选 `kb_adult` → top_k 3 / score 0.5 → 保存
4. **右上角点「发布」**（Dify 老 bug：预览不刷新，必须发布）

> 验证（见 §4.5 验收清单）：预览里问「安全期是哪几天」，回答应出现「《中国公民健康素养 2024》」引用，且元数据 `retriever_resources` 非空。
> 若仍空 → 见 `KB_ADULT_REPAIR_GUIDE.md` 的「修法 A（删重建·经济）/ 修法 B（配 embedding）/ 兜底回滚」三档方案。

### Step 5 · 测试 + 发布
1. 用 `eval_testset.csv` 里 5 条快速验证（避孕/羞耻/SM安全/边界/拒色情）
2. 点「**发布**」→ 拿到「**Web App 嵌入 / API**」

---

## 2.5 条件分支 + 直接回复（截图级操作清单）

> 你的画布现在是：`开始 → 知识检索 → 条件分支 → LLM → 直接回复`（一条线）。
> **问题**：条件分支的 IF 和 ELSE 两条线都接到了同一个 LLM，等于没分流。
> **目标**：让 ELSE 线接到一个**独立的「直接回复」节点**（兜底话术，**不走 LLM**）。

### 操作步骤（照着点）

1. **拖一个新「直接回复」节点**：左侧节点栏找到「直接回复」，拖一个到画布空白处（放在条件分支下方、LLM 旁边）。现在画布上有两个「直接回复」：一个是 LLM 后面的（主答案），一个是新拖的（兜底）。
2. **点中「条件分支」节点** → 右侧面板出现 **IF 条件** 和 **ELSE** 两条分支线（通常一条绿/蓝、一条橙/灰）。
3. **把 ELSE 那条线的终点，从 LLM 改拖到新拖来的「直接回复」节点**：
   - 鼠标悬到 ELSE 线落在 LLM 上的那个小圆点 → 按住拖到新节点上松开。
   - 改完：IF → LLM，ELSE → 新直接回复（两条线分开了）。
4. **点中新「直接回复」节点** → 右侧「回复内容」框填下方兜底话术。
5. **点「预览」** → 输入一句知识库里没有的冷门问题（如"你怎么看量子纠缠"）→ 应走 ELSE → 返回兜底话术，而非 LLM 硬答。

### 条件分支 IF 条件怎么填（点条件分支节点，找 IF 那栏）

- **变量**：选 `{{#知识检索.result#}}`（即知识检索节点的输出变量）
- **运算符**：`不为空` / `exists` / `not empty`（三个名字都行）
- **含义**：检索到了内容 → 走 IF → LLM 基于资料回答；没检索到 → 走 ELSE → 兜底话术。

### 兜底话术（填进新「直接回复」节点）

```
抱歉，这个问题我暂时没有找到很权威的资料支撑。
你可以换种更具体的说法（比如具体症状、情境，或想了解哪方面），
我会尽量基于 WHO、国家疾控等公开权威资料帮你理清楚。
如果你有紧急情况，请直接联系专业机构或医生。
```

### 验证

- 输入知识库有答案的问题（如"避孕套破裂怎么办"）→ 应走 IF → LLM 回答。
- 输入冷门无关问题 → 应走 ELSE → 兜底话术。

> MVP 阶段先到这一步即可。自伤/家暴的"安全分流"是进阶版（需先加一个 LLM 意图分类节点），等基础链路稳定后再加，见 §5 之后单独补。

---

## 3. 未成年版搭建（双版本第二步）

> **命名澄清（重要）**：「乐天知性·成年版 / 青少版」是 **Dify 后端的「两个隔离子应用」命名**，**不是两个割裂产品**。
> - **对外**：用户只看到一个入口「乐天知性」（index.html / Cloudflare Pages），过 18+ 年龄门后前端路由到对应界面。
> - **对内**：后端拆成两个物理隔离的 Dify 应用 —— 青少版应用**根本不挂** `kb_adult`、**不配置** SM/论坛/成人展节点。即使前端年龄门被绕过，未成年也拿不到成年内容（纵深防御）。
> - 详见 `DUAL_VERSION_DESIGN.md` 的「双轨分流」与「知识库分层物理隔离」。

> 依赖 `SYSTEM_PROMPT_MINOR.md`（方向调整确认后我会建）。步骤同上，区别：
- 应用名：`乐天知性·青少版`
- 提示词：`SYSTEM_PROMPT_MINOR.md`（**零色情/零性暗示/零SM**，含适龄分级）
- 知识库：`kb_minor`（刘文利读本/UNESCO CSE/保护豆豆/LoveMatters 青春版/青爱工程）
- 应用级开关：**关闭「联网搜索」、关闭「代码执行」**，仅限知识库检索，防越权

---

## 4. 自动评测（交给我跑，你只需给 Key）

你从 Dify 应用的「**API 访问**」页生成 **App API Key**（注意：是 Dify 应用密钥，**不是**阶跃 Key），发给我（仅本会话内存），我执行：
- `eval_runner.py`：读 `eval_testset.csv` → 批量调 Dify API → 落 `eval_results.json`
- 红队：`redteam_runner.py`：跑 `redteam_prompts.csv` → 出拦截率
- 三组对照（裸模型 / 成年版 / 青少版）→ 5 维评分表

> 我不会用这个 Key 做任何部署、不会写入文件、不会转发。跑完即清。

---

## 4.5 Day 8 跑通验收清单 + 交接给我跑分

### 你照着点做完后，逐项打勾（这是 A5 闭环的硬验收）
- [ ] `kb_adult` 索引模式 = **经济**（不是高质量）
- [ ] 两个 md 文档状态 = **已完成**（不是"处理中"）
- [ ] 成年版 Chatflow「知识检索」节点已重新绑定 `kb_adult` + top_k 3 / score 0.5
- [ ] 成年版已点「**发布**」
- [ ] 预览提问「安全期是哪几天」→ 回答含健康素养引用 + `retriever_resources` 非空 ✅
- [ ] （可选）青少版 `kb_minor` 同样用经济模式 + 已发布

### 验收通过后，发我任意一个信号，我立刻接 `eval_runner` 跑 152+47 基线
- **"已删重建 / kb_adult 已通"** → 我复测检索 → 跑成年版 152 评测 + 47 红队
- **"懒得配，回退到直接对话模式"** → 我改 Chatflow（拆检索节点）+ prompt 加免责声明，先拿基线
- 你也可自己跑（Key 走环境变量，**不落库**）：
  ```powershell
  cd 安心答-GOAI
  $env:DIFY_API_KEY="app-xxxx"
  python eval_runner.py --label "B Dify版" --version adult --suite eval_testset.csv --out scores/b_dify_eval.json
  python eval_runner.py --label "B Dify版" --version adult --suite redteam_prompts.csv --out scores/b_dify_redteam.json
  python eval_scorer.py --result "B Dify版=scores/b_dify_eval.json" --result "B Dify版红队=scores/b_dify_redteam.json" --outdir scores --tag dify_v1
  ```
  跑完把 `scores/` 下两个 JSON 发我，我来出 5 维评分表 + 拦截率/得分图表。

> ⚠️ 这一步是 **Day 3 真实跑分 / Day 4 红队实测 / Day 9 报告回填实测数据** 的**唯一前置阻塞**。你打通 kb_adult，我这边红队评分器链路（Day 3 已就绪）立刻能出真数据，Day 4/Day 9 缺口一并补上。

---

## 5. 提交前自检 Checklist

- [ ] 两个应用均已发布（成年版 + 青少版）
- [ ] 身份认证路由打通（未成年/成年分流）
- [ ] 评测实跑有数据（非只有测试集）
- [ ] 红队拦截率 ≥ 95%（S3/S4/S10/S11 类零漏拦）
- [ ] README + REPORT 回填实测数据
- [ ] GitHub 仓库整理（MIT，含本目录全部文件）
