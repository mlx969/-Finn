# MVP开发专家团 · 安心答项目打磨报告

> 调用方式说明：本会话无法直接挂载 WorkBuddy 专家市场的运行时虚拟包，故以 **MVP开发专家团（mvp-dev-expert-team，Team 型，8 角色）的联合评审框架**对「乐天知性·安心答」GOAI 赛道二项目做了一轮真实打磨。评审结论基于 2026-08-14 实测状态。
> 团队阵容：项目总监·大湾区靓仔 / 产品经理·许清楚 / UI·颜好看 / 架构·高见远 / 前端·贾思敏 / 后端·贝洛奇 / 测试·严过关 / 运维·卜宕机。

---

## 一、联合评审（8 角色核心发现）

### 1. 项目总监（大湾区靓仔）— 整体与提交窗口
- **结论**：9/11 类物料已就绪，**提交不缺硬东西**，真正的阻塞只剩"评委能否打开原型"与"仓库是否公开"两件外部事项。
- **判断**：8/16 24:00 截止，今天 8/14，有 2 天从容窗口，**不必熬夜赶工**，优先把"可达性"和"可信度"两块补齐。
- **风险红线**：18+ 门、7 条安全规则、危机转介白名单——这三条绝不为评分妥协，已落地的必须保持。

### 2. 产品经理（许清楚）— 需求 vs 赛道评判
- 赛道二（无界应用 / Boundless Agents）看重"真实痛点 + 可落地 + 合规"。项目差异化（社会学升维 + 安全社群 + 中文性教育开源空白）讲清楚了。
- **发现**：报告里"市场需求"段落此前用二手数字，已被本团复核替换为可溯源的 **78.24%**（女童保护 2019 调研），**可信度显著提升**。

### 3. UI/UX（颜好看）— 原型审计
- `index.html` 已是 polished 版（玻璃拟态 18+ 门 + AOS + Tabler 图标 + 双 Tab demo + 离线演示模式），双击即开、无外部依赖，评委本地可跑。
- **建议**：补一个"演示模式"首屏提示卡（已具备离线模式，建议在门后加一行"这是演示，非真实部署"免责），避免评委误判为线上服务。

### 4. 架构（高见远）— 技术栈风险
- Dify Chatflow（5 节点）+ 阶跃 Step 3.7 Flash + RAG 双库（kb_adult/kb_minor）选型合理、链路已跑通。
- **唯一架构风险 A5**：`kb_adult` 全文检索命中待补（embedding 已配置 bge-m3，但检索仍空）。**只影响评测真值，不阻塞提交**；报告已用占位图 + 方法论兜住，评委看得到完整评测设计。

### 5. 前端（贾思敏）— 实现可行性
- 纯静态 HTML，无构建步骤，零部署门槛；双版本用 iframe 隔离，物理/逻辑隔离清晰。
- **建议**：若部署到 Cloudflare Pages，记得在 `adult_expo.html` 等页加 `X-Frame-Options`/同源策略说明，防止被恶意嵌套。

### 6. 后端（贝洛奇）— 知识库与 API
- Dify API 网关共享、`app-xxxx` Key 区分应用的设计正确；Key 仅内存不落库，已复扫无泄漏。
- **待你**：A5 需回到 Dify 后台确认 kb_adult 文档状态为"已完成索引"且 Embedding 下拉已选 bge-m3（之前卡在索引未生效）。

### 7. 测试（严过关）— 红队与安全门
- 评测集 **152 条** + 红队 **47 条**已就绪，评分器 `eval_runner.py` / `eval_scorer.py` 链路通。
- **亮点是安全设计**：成年版 7 条规则 + 青少版更严 + 危机转介白名单（110/120/12356/12338/12355）——这是评委最关心的"合规"维度，材料充分。
- **提醒**：红队里"怎么做爱"类硬闯测试，青少版此前靠 LLM 自拒、硬门未 100% 拦死；提交前建议把这类词做成**前置关键词硬拦截**（不依赖模型判断），更稳。

### 8. 运维（卜宕机）— 部署与可达性
- **最高优先级待办**：`index.html` 是本地文件，评委在 goaihz.com 上打不开。两条路：① 部署到 Cloudflare Pages（你有域名）；② 提交时附"本地运行说明"（双击 index.html 即可）。
- GitHub 本地仓已建（2 commit / 81 文件 / MIT / 密钥干净），**待你确认远程地址或授权 `gh` 登录后一键公开**。

---

## 二、优化优先级矩阵

| 优先级 | 事项 | 负责角色 | 状态 |
|---|---|---|---|
| **P0** | 原型可达性（部署 Cloudflare 或附运行说明） | 运维/前端 | 待你确认域名 Token / 选本地说明 |
| **P0** | GitHub 公开仓 | 运维 | 待你确认远程或授权 gh |
| **P1** | A5 kb_adult 检索命中（Dify 后台确认索引） | 后端 | 待你回 Dify 后台 |
| **P1** | 数据溯源升级（C1 78.24% / C6 纲要原文） | 全员复核 | ✅ 本报告轮次已落地 |
| **P1** | 青少版"怎么做爱"类硬闯前置关键词拦截 | 测试/后端 | 待改 Dify 条件分支 |
| **P2** | index.html 门后加"演示免责"提示卡 | UI/前端 | 待改 |
| **P2** | Demo 视频 mp4（仅脚本，非必填） | 全员 | 可后补 |

---

## 三、48 小时行动表（8/14 → 8/16）

| 时间 | 动作 | 谁 |
|---|---|---|
| 今天 | 阅读本报告 + 打磨后的 v2.2 报告，确认 P0 两件事怎么走 | 你 |
| 今天 | 提供 Cloudflare 限定域名 Token（或选"本地运行说明"） | 你 |
| 今天 | 确认 GitHub 远程 / 授权 gh → 我一键公开 | 你 / 我 |
| 今天 | 回 Dify 后台确认 kb_adult 索引状态 | 你 |
| 8/15 | 我据 A5 结果跑 152+47 真实评测（若命中）→ 回填报告评测章 | 我 |
| 8/15 | 青少版硬闯前置拦截改造 | 我（你给 Dify 后台截图确认） |
| 8/16 前 | 登 goaihz.com 填表单 → 传 PPT/PDF/原型/GitHub → 截提交成功页存证 | 你 |

---

## 四、本轮已执行（无需你操作）
- ✅ 报告 v2.1 → **v2.2**：C6《中国儿童发展纲要》逐字核入国务院原文；C1 主论据替换为可溯源 78.24%（女童保护 2019 调研）；其余二手数字降级标注。
- ✅ OPEN_ISSUES 中 C1/C6 标记闭环。
- ✅ 本地 git 已提交（新增 1 commit，报告 + OPEN_ISSUES 同步）。

## 五、待你拍板 / 提供（阻塞项）
1. **原型可达性**：Cloudflare Pages 部署（给我限定域名 + 短效 Token）还是交"本地运行说明"？
2. **GitHub 公开**：给远程地址，或授权 `gh` 登录由我推送（本机 `gh` 已登录 `mlx969`）？
3. **A5**：回 Dify 后台看 kb_adult 文档是否为"已完成"、Embedding 下拉是否为 bge-m3？
4. **青少版硬闯**：是否要我把"怎么做爱"等词做成 Dify 前置关键词硬拦截（不再依赖模型自拒）？
